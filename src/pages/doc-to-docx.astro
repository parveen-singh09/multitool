---
import ToolLayout from '../layouts/ToolLayout.astro';
import ArchiveConvert from '../components/ArchiveConvert.astro';
---

<ToolLayout slug="doc-to-docx">
  <ArchiveConvert slug="doc-to-docx" from="doc" to="docx" noun="document" />

  <div slot="about" class="flex flex-col gap-4 text-[15px] leading-relaxed text-ink-subtle">
    <p>
      This free DOC to DOCX converter converts legacy Microsoft Word DOC files to the modern DOCX format.
    </p>
    <p>
      Conversion is handled on our servers, because these
      document formats can't be read reliably in a browser. Your file is uploaded
      to our servers, converted, and returned
      as a download link.
    </p>
  </div>
</ToolLayout>
