---
import ToolLayout from '../layouts/ToolLayout.astro';
import RasterBatch from '../components/RasterBatch.astro';
---

<ToolLayout slug="bmp-to-base64">
  <RasterBatch to="base64" accept="image/bmp,.bmp" fromLabel="BMP" />

  <div slot="about" class="flex flex-col gap-4 text-[15px] leading-relaxed text-ink-subtle">
    <p>
      This free BMP to Base64 converter encodes each BMP as a base64 data URI you can paste straight into HTML, CSS or a JSON payload — no image hosting needed.    </p>
    <p>
      Drop a single image or a whole batch at once, optionally resize by the longest side,
      and preview each result before you download. Conversions run through the browser's canvas.
    </p>
  </div>
</ToolLayout>
