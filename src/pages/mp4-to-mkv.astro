---
import ToolLayout from '../layouts/ToolLayout.astro';
import VideoConvert from '../components/VideoConvert.astro';
---

<ToolLayout slug="mp4-to-mkv">
  <VideoConvert slug="mp4-to-mkv" from="mp4" to="mkv" />

  <div slot="about" class="flex flex-col gap-4 text-[15px] leading-relaxed text-ink-subtle">
    <p>
      This free MP4 to MKV converter repackages MP4 videos into the flexible Matroska MKV container.
    </p>
    <p>
      This converter uses ffmpeg compiled to WebAssembly. Re-encoding is
      CPU-intensive, so expect roughly real-time speed.
    </p>
  </div>
</ToolLayout>
