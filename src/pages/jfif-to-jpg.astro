---
import ToolLayout from '../layouts/ToolLayout.astro';
import RasterBatch from '../components/RasterBatch.astro';
---

<ToolLayout slug="jfif-to-jpg">
  <RasterBatch to="image/jpeg" accept="image/jpeg,.jfif,.jpg,.jpeg" fromLabel="JFIF" quality={true} matte={true} />

  <div slot="about" class="flex flex-col gap-4 text-[15px] leading-relaxed text-ink-subtle">
    <p>
      This free JFIF to JPG converter re-encodes .jfif files (which are just JPEGs with a different extension) into standard .jpg images that every app and website accepts.
    </p>
    <p>
      Drop a single image or a whole batch at once, optionally resize by the longest side,
      and preview each result before you download.
    </p>
  </div>
</ToolLayout>
