---
import ToolLayout from '../layouts/ToolLayout.astro';
import DocConvert from '../components/DocConvert.astro';
---

<ToolLayout slug="ipynb-to-html">
  <DocConvert slug="ipynb-to-html" outLabel="HTML" />

  <div slot="about" class="flex flex-col gap-4 text-[15px] leading-relaxed text-ink-subtle">
    <p>
      This free IPYNB to HTML converter converts a Jupyter notebook (.ipynb) into an HTML document. Everything runs entirely in your browser, so your files are never uploaded to a server.
    </p>
    <p>
      Paste your content or attach a file, click Convert, then download or copy the result.
      Conversions happen offline through your browser, so the tool works without a network
      connection and nothing leaves your device.
    </p>
  </div>
</ToolLayout>
